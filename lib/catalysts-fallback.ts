// Synthetic earnings/dividends previously lived here as placeholder data.
// They were unsourced and could be wrong. Removed.
import type { Catalyst } from "./catalysts";
export function syntheticFor(_ticker: string): Catalyst[] {
  return [];
}
