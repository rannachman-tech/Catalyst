# Optional. With a Finnhub key, earnings + dividend dates become live.
# Without it, the app falls back to curated synthetic data so the
# experience still works on first paint.
FINNHUB_API_KEY=

# Optional. Override "today" for demos (e.g. EC_TODAY_OVERRIDE=2026-05-05).
EC_TODAY_OVERRIDE=
